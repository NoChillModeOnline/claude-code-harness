# Language / i18n Configuration

Closes: [Issue #173](https://github.com/Chachamaru127/claude-code-harness/issues/173) — English version / how to set the global output language.

User-facing responses (skill outputs, slash commands, hook messages) follow the explicit session or project language. **When no language is configured, the default is English.** Use Japanese only when explicitly requested via one of the three mechanisms below.

This file is the SSOT. `CLAUDE.md` Language section is a short pointer to here. The README links here under "Language".

---

## How to switch language

There are three ways to switch the language. The locale resolver
(`scripts/config-utils.sh:get_harness_locale`, also reflected in
`go/internal/hookhandler/helpers.go`) applies them in this precedence:

```
1. Explicit override (function arg, used by setup hook)
2. .claude-code-harness.config.yaml      ← i18n.language
3. CLAUDE_CODE_HARNESS_LANG               (env var)
4. en                                     (default)
```

Project config wins over the env var — set the env var only for projects that
do not yet have a config file, or temporarily override behaviour that the file
should not.

Per-message session instructions to the agent are a separate path: they
influence how the LLM phrases its reply, but they do **not** flow through the
hook-side locale resolver above.

### 1. Project-level config — `.claude-code-harness.config.yaml`

For a per-project setting that persists across sessions, edit the project's
`.claude-code-harness.config.yaml`:

```yaml
i18n:
  language: ja   # ja | en (default: en)
```

This applies to every Claude Code session run inside the project. Commit the
file to make the choice repository-wide. The setup hook (`harness init`)
generates this file from the locale-aware template at install time.

### 2. Per-session env var — `CLAUDE_CODE_HARNESS_LANG`

For one-off sessions (or for projects that do not yet have a config file),
export the env var before launching Claude Code:

```bash
export CLAUDE_CODE_HARNESS_LANG=ja
claude            # this session uses Japanese
```

Or inline:

```bash
CLAUDE_CODE_HARNESS_LANG=ja claude
```

Reminder: if the project's `.claude-code-harness.config.yaml` sets
`i18n.language`, that value wins over the env var. Remove or edit the config
file when you want the env var to take effect.

### 3. Per-message session instruction

For ad-hoc switching mid-session, tell the agent in your own words:

```
> 以後は日本語で返答してください
```

```
> Please respond in English from now on.
```

This affects the model's prose for the rest of the session but does not change
the locale resolver result that hooks and contracts read.

---

## What does NOT change

- Machine-readable values (JSON schema fields, contract verdicts, log keys) stay English regardless of the language setting.
- Source code identifiers, file names, commit hashes, command names stay as-is.
- Conventional Commits prefixes (`feat:` / `fix:` / `docs:` ...) stay English.

If you see Japanese in a place that should be machine-readable (e.g. a JSON contract field), please open an Issue — that's a bug, not an i18n setting question.

---

## Supported languages

| Code | Status |
|---|---|
| `en` | Supported (default) |
| `ja` | Supported |
| other (`zh`, `ko`, `de`, etc.) | Not officially supported — session instruction can request best-effort, but skill outputs may include English fallbacks |

---

## Related

- `CLAUDE.md` § Language (short pointer to this file)
- `README.md` § Language (link to this file)
- `.claude-code-harness.config.yaml` (`i18n.language` key)
- `scripts/config-utils.sh` (`get_harness_locale` precedence)
- `go/internal/hookhandler/helpers.go` (Go-side resolver mirror)
